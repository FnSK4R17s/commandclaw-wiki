# Log

Append-only record of ingests, queries, and lint passes.
Entry prefix: `## [YYYY-MM-DD] ingest | <subject>` (also `query`, `lint`).

Recent entries: `grep "^## \[" wiki/log.md | tail -10`

---

_No entries yet._
